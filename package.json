{
  "name": "orida",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./script/server",
    "lint:front:fix": "cd ./frontend && yarn lint:fix",
    "lint:back:fix": "cd ./backend && yarn lint:fix",
    "lint:fix": "yarn lint:back:fix && yarn lint:front:fix",
    "deploy:staging": "cd ./environments/staging && terraform apply"
  }
}

{
  "name": "orida",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "./script/server",
    "deploy:staging": "cd ./environments/staging && terraform apply"
  }
}
